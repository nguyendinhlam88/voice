# voice

Discovery config + releases cho **Voice Studio** (desktop TTS / voice cloning).

- `voice-config.json` — license_url + version + download_url (ký Ed25519), app đọc lúc khởi động.
- Releases — file cài `VoiceStudio-Setup.exe`.
